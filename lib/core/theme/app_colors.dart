import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ============================================================
  // HANDZY THOZHAN BRAND COLORS
  // ============================================================

  /// Main Handzy brand color
  static const Color primary = Color(0xFF7A0E23);

  /// Secondary maroon color
  static const Color secondary = Color(0xFFA83248);

  // ============================================================
  // BACKGROUND
  // ============================================================

  /// Main application background
  static const Color background = Color(0xFFFDEEEE);

  /// Very light maroon background for cards / sections
  static const Color lightTeal = Color(0xFFF8E4E7);

  // ============================================================
  // TEXT
  // ============================================================

  /// Main dark text
  static const Color textPrimary = Color(0xFF2D1F21);

  /// Secondary text
  static const Color textSecondary = Color(0xFF6F5B60);

  /// Text on primary buttons
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ============================================================
  // STATUS
  // ============================================================

  /// Success
  static const Color success = Color(0xFF2E8B57);

  /// Warning
  static const Color warning = Color(0xFFE6A23C);

  /// Error
  static const Color error = Color(0xFFD62839);

  // ============================================================
  // UI HELPERS
  // ============================================================

  /// Light maroon border
  static const Color border = Color(0xFFE8D7DA);

  /// Soft white / pink card
  static const Color card = Color(0xFFFFF7F7);
}