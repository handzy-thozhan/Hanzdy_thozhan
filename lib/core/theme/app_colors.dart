import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ============================================================
  // HANDZY THOZHAN BRAND COLORS
  // ============================================================

  /// Main Handzy brand color
  static const Color primary = Color(0xFF009375);

  /// Bright secondary / animation color
  static const Color secondary = Color(0xFF00EBC9);

  // ============================================================
  // BACKGROUND
  // ============================================================

  /// Main application background
  static const Color background = Color(0xFFFFFFFF);

  /// Very light teal background for cards / sections
  static const Color lightTeal = Color(0xFFE8FFFA);

  // ============================================================
  // TEXT
  // ============================================================

  /// Main dark text
  static const Color textPrimary = Color(0xFF101820);

  /// Secondary text
  static const Color textSecondary = Color(0xFF667085);

  /// Text on primary buttons
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ============================================================
  // STATUS
  // ============================================================

  /// Success
  static const Color success = Color(0xFF009375);

  /// Warning
  static const Color warning = Color(0xFFFFA726);

  /// Error
  static const Color error = Color(0xFFE53935);

  // ============================================================
  // UI HELPERS
  // ============================================================

  /// Light border
  static const Color border = Color(0xFFE4E7EC);

  /// White card
  static const Color card = Color(0xFFFFFFFF);
}